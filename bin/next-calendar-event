#!/bin/bash
EVENT=`gcalcli --locale en_US --nostarted --calendar "" agenda --nocolor | cut -d " " -f 4- | head -2 | tail -1 | sed "s/^ *//g" | sed "s/    / /g" | cut -c1-19`

if [ -z "$EVENT" ]
then
  echo "No events today"
else
  echo $EVENT
fi
