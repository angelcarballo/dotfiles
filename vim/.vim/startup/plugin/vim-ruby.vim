"" Vim-ruby options
let ruby_spellcheck_strings=1      " enable spellcheck inside ruby strings
let ruby_minlines=500              " avoid syntax errors while scrolling on large files
let g:ruby_indent_block_style='do' " better syntax for nested blocks
let ruby_foldable_groups='def'     " only fold methods
let ruby_fold=1                    " enable folds
